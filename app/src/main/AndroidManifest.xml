<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QfindeR"
        tools:targetApi="31">
        <activity
            android:name=".controller.CrearNota"
            android:exported="false" />
        <activity
            android:name=".controller.RegistroUsuario"
            android:exported="false" />
        <activity
            android:name=".controller.Terminos"
            android:exported="false" />
        <activity
            android:name=".controller.Login"
            android:exported="false" />
        <activity
            android:name=".controller.Menu_inicio_sesion"
            android:exported="false" />
        <activity
            android:name=".controller.MainActivity"
            android:exported="false" />
        <activity
            android:name=".controller.ListaNotas"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>